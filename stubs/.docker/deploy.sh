#!/bin/sh
set -e

echo '------ Start deploy tasks  ------'

# ------------- Find your way! ------------ #

# cp .env.example .env
# composer install --prefer-dist --no-interaction --no-progress --ansi --optimize-autoloader --no-dev
# yarn install
# yarn build
# php artisan config:cache
# php artisan view:cache
# php artisan route:cache
# php artisan storage:link
# php artisan migrate --seed --force 

echo '------ Deploy tasks ended ------'